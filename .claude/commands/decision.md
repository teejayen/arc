---
description: Extract a decision to decisions/ folder
---

Extract and document a decision from this conversation.

Use the **decision-capture** skill (`.claude/skills/decision-capture/SKILL.md`) to:

1. Identify the decision (from `$ARGUMENTS` or ask)
2. Create decision file in `decisions/YYYY-MM-DD-decision-topic.md`
3. Include context, options considered, reasoning
4. Confirm location and remind `/session` will push
